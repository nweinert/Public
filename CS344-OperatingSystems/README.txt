To compile smallsh using the makefile, enter "make" at the command prompt.
To compile smallsh using command line arguments, type gcc -g -Wall -std=c99 -o smallsh smallsh.c -lpthread

After compiling smallsh can be run by entering ./smallsh at the command prompt
